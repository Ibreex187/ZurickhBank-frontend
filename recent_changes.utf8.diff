diff --git a/src/App.css b/src/App.css
index 3d9d6df..92f9b53 100644
--- a/src/App.css
+++ b/src/App.css
@@ -2,3 +2,25 @@
   width: 100%;
   min-height: 100vh;
 }
+
+html,
+body {
+  width: 100%;
+  max-width: 100%;
+  min-height: 100%;
+  margin: 0;
+  overflow-x: hidden;
+}
+
+*,
+*::before,
+*::after {
+  box-sizing: border-box;
+}
+
+img,
+svg,
+video,
+canvas {
+  max-width: 100%;
+}
diff --git a/src/pages/AdminDashboard.styles.js b/src/pages/AdminDashboard.styles.js
index 19b1e70..39ffb51 100644
--- a/src/pages/AdminDashboard.styles.js
+++ b/src/pages/AdminDashboard.styles.js
@@ -48,11 +48,17 @@ ${DASHBOARD_STYLES}
 .premium-stat-card h2 {
   font-weight: var(--font-weight-bold);
   max-width: 100%;
+  display: inline-block;
   overflow-wrap: anywhere;
   word-break: break-word;
   font-variant-numeric: tabular-nums;
 }
 
+.admin-dashboard-page .premium-stat-card .d-flex {
+  gap: var(--space-md);
+  flex-wrap: wrap;
+}
+
 .premium-stat-card i {
   color: var(--accent); /* Electric Blue icons */
 }
@@ -66,6 +72,11 @@ ${DASHBOARD_STYLES}
   border: 1px solid var(--border-light);
 }
 
+.admin-dashboard-page .table-responsive {
+  overflow-x: auto;
+  -webkit-overflow-scrolling: touch;
+}
+
 .admin-dashboard-page .table thead th {
   border: none;
   font-weight: var(--font-weight-semibold);
@@ -217,6 +228,16 @@ ${DASHBOARD_STYLES}
     padding-bottom: var(--space-xl) !important;
   }
 
+  .admin-dashboard-page .d-flex.justify-content-between.align-items-center.mb-3 {
+    flex-direction: column;
+    align-items: flex-start !important;
+    gap: var(--space-sm);
+  }
+
+  .admin-dashboard-page .table {
+    min-width: 620px;
+  }
+
   .premium-stat-card h2 {
     font-size: clamp(1.15rem, 5.2vw, 1.6rem);
     line-height: 1.25;
diff --git a/src/pages/BeneficiaryManagement.styles.js b/src/pages/BeneficiaryManagement.styles.js
index 2fa9930..35d03e3 100644
--- a/src/pages/BeneficiaryManagement.styles.js
+++ b/src/pages/BeneficiaryManagement.styles.js
@@ -375,6 +375,8 @@ ${DASHBOARD_STYLES}
   margin: 0;
   display: flex;
   justify-content: space-between;
+  align-items: flex-start;
+  flex-wrap: wrap;
   gap: var(--space-sm);
   color: var(--text-main);
   font-size: var(--font-sizes-sm);
@@ -385,12 +387,18 @@ ${DASHBOARD_STYLES}
 }
 
 .beneficiary-limit-line strong {
+  min-width: 0;
   font-variant-numeric: tabular-nums;
   overflow-wrap: anywhere;
   word-break: break-word;
   text-align: right;
 }
 
+.beneficiary-management .table-responsive {
+  overflow-x: auto;
+  -webkit-overflow-scrolling: touch;
+}
+
 .beneficiary-limit-line.warning strong {
   color: var(--warning);
 }
@@ -536,6 +544,10 @@ ${DASHBOARD_STYLES}
     padding: var(--space-md);
   }
 
+  .beneficiary-management .table {
+    min-width: 620px;
+  }
+
   .premium-stat-card h3 {
     font-size: clamp(1.05rem, 4.8vw, 1.35rem);
     line-height: 1.25;
@@ -556,6 +568,15 @@ ${DASHBOARD_STYLES}
     flex-direction: column;
   }
 
+  .beneficiary-card {
+    padding: var(--space-lg);
+  }
+
+  .beneficiary-actions {
+    flex-direction: column;
+    align-items: stretch;
+  }
+
   .beneficiary-limit-header {
     flex-direction: column;
     align-items: flex-start;
diff --git a/src/pages/Dashboard.styles.js b/src/pages/Dashboard.styles.js
index 0bd52ba..fa47c9b 100644
--- a/src/pages/Dashboard.styles.js
+++ b/src/pages/Dashboard.styles.js
@@ -125,6 +125,13 @@ body {
   background: var(--white);
   padding: calc(var(--space-lg) + 2px) var(--space-xl);
   box-shadow: 0 1px 0 rgba(15,23,42,0.03);
+  gap: var(--space-md);
+  flex-wrap: wrap;
+}
+
+.main-header .header-left,
+.main-header .header-right {
+  min-width: 0;
 }
 
 .page-title {
@@ -391,6 +398,7 @@ body {
   margin-left: 280px;
   background: var(--silver-light);
   overflow-y: auto;
+  min-width: 0;
 }
 
 .sidebar.collapsed ~ .main-content {
@@ -691,6 +699,8 @@ body {
   border: 1px solid var(--border-light);
   padding: var(--space-sm) var(--space-md);
   border-radius: var(--radius-md);
+  min-width: 0;
+  max-width: 100%;
 }
 
 .user-avatar {
@@ -708,12 +718,17 @@ body {
 .user-info {
   display: flex;
   flex-direction: column;
+  min-width: 0;
 }
 
 .user-name {
   font-size: 13px;
   font-weight: 700;
   color: var(--navy);
+  max-width: 100%;
+  overflow: hidden;
+  text-overflow: ellipsis;
+  white-space: nowrap;
 }
 
 .user-role {
@@ -767,6 +782,8 @@ body {
   display: flex;
   justify-content: space-between;
   align-items: center;
+  gap: var(--space-md);
+  flex-wrap: wrap;
   margin-bottom: var(--space-xl);
 }
 
@@ -809,6 +826,7 @@ body {
   flex-wrap: wrap;
   gap: 6px;
   margin-bottom: var(--space-md);
+  min-width: 0;
 }
 
 .balance-amount .currency {
@@ -822,6 +840,7 @@ body {
   font-weight: 800;
   line-height: 1;
   max-width: 100%;
+  display: inline-block;
   overflow-wrap: anywhere;
   word-break: break-word;
   font-variant-numeric: tabular-nums;
@@ -830,6 +849,7 @@ body {
 .balance-change {
   display: flex;
   align-items: center;
+  flex-wrap: wrap;
   gap: var(--space-md);
 }
 
@@ -931,6 +951,8 @@ body {
   color: var(--text-main);
   display: flex;
   justify-content: space-between;
+  align-items: flex-start;
+  flex-wrap: wrap;
   gap: var(--space-sm);
 }
 
@@ -939,6 +961,7 @@ body {
 }
 
 .limit-line strong {
+  min-width: 0;
   font-variant-numeric: tabular-nums;
   overflow-wrap: anywhere;
   word-break: break-word;
@@ -1840,7 +1863,7 @@ body {
 @media (max-width: 768px) {
   /* Use transform-based off-canvas so we can animate and keep accessibility */
   .sidebar {
-    width: 280px;
+    width: min(280px, 84vw);
     transform: translateX(-100%);
     transition: transform 240ms ease-in-out;
     position: fixed;
@@ -1894,14 +1917,25 @@ body {
   .quick-actions,
   .transaction-form-section,
   .transactions-section {
-    padding-left: var(--space-lg);
-    padding-right: var(--space-lg);
+    padding-left: var(--space-md);
+    padding-right: var(--space-md);
   }
 
   .main-header {
     flex-direction: column;
     align-items: flex-start;
     gap: var(--space-md);
+    padding-top: var(--space-lg);
+    padding-bottom: var(--space-lg);
+  }
+
+  .main-header .header-left,
+  .main-header .header-right {
+    width: 100%;
+  }
+
+  .main-header .header-right {
+    justify-content: flex-start;
   }
 
   .header-left,
@@ -1911,6 +1945,14 @@ body {
     max-width: 100%;
   }
 
+  .user-profile {
+    width: 100%;
+  }
+
+  .user-avatar {
+    flex-shrink: 0;
+  }
+
   .page-title {
     font-size: 22px;
     line-height: 1.25;
@@ -1923,6 +1965,12 @@ body {
     word-break: break-word;
   }
 
+  .user-name {
+    white-space: normal;
+    overflow: visible;
+    text-overflow: clip;
+  }
+
   .balance-amount .currency {
     font-size: 20px;
   }
@@ -1931,6 +1979,17 @@ body {
     font-size: clamp(24px, 8vw, 34px);
   }
 
+  .balance-card {
+    padding: var(--space-lg);
+  }
+
+  .balance-card .card-header,
+  .balance-change,
+  .limit-line {
+    flex-direction: column;
+    align-items: flex-start;
+  }
+
   /* Mobile menu button - visible on small screens */
   .mobile-menu-btn {
     display: inline-flex;
@@ -1953,7 +2012,8 @@ body {
   /* Place the mobile menu button to the left of the header content */
   .main-header .header-left {
     display: flex;
-    align-items: center;
+    align-items: flex-start;
+    flex-wrap: wrap;
     gap: var(--space-md);
   }
 
@@ -1962,8 +2022,8 @@ body {
   }
 
   .limits-overview {
-    margin-left: var(--space-lg);
-    margin-right: var(--space-lg);
+    margin-left: var(--space-md);
+    margin-right: var(--space-md);
   }
 
   .limits-overview-header {
@@ -1999,6 +2059,10 @@ body {
     margin-right: 0;
   }
 
+  .table-container {
+    -webkit-overflow-scrolling: touch;
+  }
+
   .pagination {
     flex-direction: column;
     align-items: flex-start;
@@ -2014,7 +2078,7 @@ body {
   }
 
   .transactions-table {
-    min-width: 640px;
+    min-width: 560px;
   }
 
   .ledger-statement-line {
@@ -2026,6 +2090,10 @@ body {
     width: 95%;
     padding: var(--space-xl);
   }
+
+  .method-cards-grid {
+    grid-template-columns: 1fr;
+  }
 }
 `;
 
diff --git a/src/pages/InvestmentPlans.styles.js b/src/pages/InvestmentPlans.styles.js
index b3afcef..3eee96b 100644
--- a/src/pages/InvestmentPlans.styles.js
+++ b/src/pages/InvestmentPlans.styles.js
@@ -77,11 +77,17 @@ ${DASHBOARD_STYLES}
 .premium-stat-card h3 {
   font-weight: var(--font-weight-bold);
   max-width: 100%;
+  display: inline-block;
   overflow-wrap: anywhere;
   word-break: break-word;
   font-variant-numeric: tabular-nums;
 }
 
+.investment-plans .premium-stat-card .d-flex {
+  gap: var(--space-md);
+  flex-wrap: wrap;
+}
+
 .premium-stat-card i {
   color: var(--accent); /* Electric Blue icons */
 }
@@ -125,6 +131,11 @@ ${DASHBOARD_STYLES}
   padding: var(--space-xl);
 }
 
+.investment-plans .investment-plan-card .card-header,
+.investment-plans .investment-plan-card .card-body {
+  min-width: 0;
+}
+
 .investment-plans .card {
   border-radius: var(--radius-md);
 }
@@ -136,6 +147,11 @@ ${DASHBOARD_STYLES}
   background: var(--white);
 }
 
+.investment-plans .table-responsive {
+  overflow-x: auto;
+  -webkit-overflow-scrolling: touch;
+}
+
 .investment-plans .table thead th {
   border: none;
   font-weight: var(--font-weight-semibold);
@@ -371,12 +387,21 @@ ${DASHBOARD_STYLES}
     padding: var(--space-md);
   }
 
+  .investment-plans .table {
+    min-width: 620px;
+  }
+
   .premium-stat-card h3,
   .investment-plans .card h2 {
     font-size: clamp(1.05rem, 4.8vw, 1.45rem);
     line-height: 1.25;
   }
 
+  .investment-plans .investment-plan-card .card-header,
+  .investment-plans .investment-plan-card .card-body {
+    padding: var(--space-lg);
+  }
+
   .investment-plans .btn {
     padding: var(--space-sm) var(--space-md);
     font-size: var(--font-sizes-xs);
diff --git a/src/pages/Profile.styles.js b/src/pages/Profile.styles.js
index 12b0218..96e4b4e 100644
--- a/src/pages/Profile.styles.js
+++ b/src/pages/Profile.styles.js
@@ -79,6 +79,8 @@ ${DASHBOARD_STYLES}
   display: flex;
   justify-content: space-between;
   align-items: center;
+  gap: var(--space-md);
+  flex-wrap: wrap;
 }
 
 .nav-left {
@@ -123,6 +125,8 @@ ${DASHBOARD_STYLES}
   display: flex;
   align-items: center;
   gap: var(--space-md);
+  min-width: 0;
+  max-width: 100%;
 }
 
 .user-avatar {
@@ -149,6 +153,8 @@ ${DASHBOARD_STYLES}
   font-weight: var(--font-weight-semibold);
   color: var(--navy);
   font-size: var(--font-sizes-base);
+  overflow-wrap: anywhere;
+  word-break: break-word;
 }
 
 /* Main Container */
@@ -221,6 +227,8 @@ ${DASHBOARD_STYLES}
   font-size: var(--font-sizes-2xl);
   font-weight: var(--font-weight-bold);
   color: var(--navy);
+  overflow-wrap: anywhere;
+  word-break: break-word;
 }
 
 .profile-email {
@@ -229,6 +237,8 @@ ${DASHBOARD_STYLES}
   display: flex;
   align-items: center;
   gap: var(--space-sm);
+  overflow-wrap: anywhere;
+  word-break: break-word;
 }
 
 .profile-status {
@@ -258,6 +268,8 @@ ${DASHBOARD_STYLES}
   background: var(--white);
   padding: 0 var(--space-xl);
   border-radius: var(--radius-lg) var(--radius-lg) 0 0;
+  overflow-x: auto;
+  -webkit-overflow-scrolling: touch;
 }
 
 .tab-button {
@@ -336,6 +348,10 @@ ${DASHBOARD_STYLES}
 .account-summary .balance-item .value {
   font-size: clamp(1.05rem, 1.2vw, 1.25rem);
   line-height: 1.2;
+  max-width: 100%;
+  overflow-wrap: anywhere;
+  word-break: break-word;
+  font-variant-numeric: tabular-nums;
 }
 
 .account-summary .account-details {
@@ -772,6 +788,19 @@ ${DASHBOARD_STYLES}
 
   .nav-container {
     padding: 0 var(--space-lg);
+    align-items: flex-start;
+  }
+
+  .nav-left,
+  .nav-right,
+  .user-info {
+    width: 100%;
+    min-width: 0;
+  }
+
+  .nav-left {
+    flex-wrap: wrap;
+    gap: var(--space-md);
   }
 
   .page-title {
@@ -781,6 +810,7 @@ ${DASHBOARD_STYLES}
   .profile-tabs {
     gap: var(--space-md);
     padding: 0 var(--space-lg);
+    flex-wrap: nowrap;
   }
 
   .tab-button {
@@ -794,6 +824,18 @@ ${DASHBOARD_STYLES}
     gap: var(--space-sm);
   }
 
+  .profile-avatar {
+    width: 96px;
+    height: 96px;
+    font-size: var(--font-sizes-3xl);
+  }
+
+  .profile-name,
+  .profile-email {
+    text-align: center;
+    justify-content: center;
+  }
+
   .account-summary .account-details {
     grid-template-columns: 1fr;
   }
diff --git a/src/pages/SavingsManagement.jsx b/src/pages/SavingsManagement.jsx
index af9c22f..2af79ac 100644
--- a/src/pages/SavingsManagement.jsx
+++ b/src/pages/SavingsManagement.jsx
@@ -529,7 +529,7 @@ const SavingsManagement = ({ styles }) => {
             </Row>
 
             {/* Recent Transactions */}
-            <Card className="shadow-sm">
+            <Card className="shadow-sm savings-recent-transactions-card">
               <Card.Header>
                 <h5 className="mb-0">Recent Transactions</h5>
               </Card.Header>
@@ -541,8 +541,8 @@ const SavingsManagement = ({ styles }) => {
                     <p className="text-muted">Start saving to see your transaction history</p>
                   </div>
                 ) : (
-                  <div className="table-responsive">
-                    <Table hover>
+                  <div className="table-responsive savings-recent-transactions-table-wrap">
+                    <Table hover className="savings-recent-transactions-table">
                       <thead className="table-light">
                         <tr>
                           <th>Date</th>
@@ -555,15 +555,15 @@ const SavingsManagement = ({ styles }) => {
                       <tbody>
                         {transactions.map((transaction) => (
                           <tr key={transaction._id || transaction.transactionId}>
-                            <td className="savings-text-cell">{new Date(transaction.createdAt).toLocaleDateString()}</td>
-                            <td>
+                            <td className="savings-text-cell" data-label="Date">{new Date(transaction.createdAt).toLocaleDateString()}</td>
+                            <td data-label="Type">
                               <Badge bg={transaction.type === 'deposit' ? 'success' : 'warning'}>
                                 {transaction.type}
                               </Badge>
                             </td>
-                            <td className="savings-amount-cell">{transaction.amount?.toLocaleString()}</td>
-                            <td className="savings-text-cell">{transaction.description}</td>
-                            <td>
+                            <td className="savings-amount-cell" data-label="Amount">{transaction.amount?.toLocaleString()}</td>
+                            <td className="savings-text-cell" data-label="Description">{transaction.description}</td>
+                            <td data-label="Status">
                               <Badge bg={transaction.status === 'completed' ? 'success' : 'secondary'}>
                                 {transaction.status}
                               </Badge>
diff --git a/src/pages/SavingsManagement.styles.js b/src/pages/SavingsManagement.styles.js
index f7ae7eb..afbe46d 100644
--- a/src/pages/SavingsManagement.styles.js
+++ b/src/pages/SavingsManagement.styles.js
@@ -67,11 +67,17 @@ ${DASHBOARD_STYLES}
 .premium-stat-card h3 {
   font-weight: var(--font-weight-bold);
   max-width: 100%;
+  display: inline-block;
   overflow-wrap: anywhere;
   word-break: break-word;
   font-variant-numeric: tabular-nums;
 }
 
+.savings-management .premium-stat-card .d-flex {
+  gap: var(--space-md);
+  flex-wrap: wrap;
+}
+
 .premium-stat-card i {
   color: var(--accent); /* Electric Blue icons */
 }
@@ -121,6 +127,18 @@ ${DASHBOARD_STYLES}
   background: var(--white);
 }
 
+.savings-management .table-responsive {
+  overflow-x: auto;
+  -webkit-overflow-scrolling: touch;
+}
+
+.savings-recent-transactions-card .card-header {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: var(--space-sm);
+}
+
 .savings-management .table thead th {
   border: none;
   font-weight: var(--font-weight-semibold);
@@ -347,6 +365,8 @@ ${DASHBOARD_STYLES}
   margin: 0;
   display: flex;
   justify-content: space-between;
+  align-items: flex-start;
+  flex-wrap: wrap;
   gap: var(--space-sm);
   color: var(--text-main);
   font-size: var(--font-sizes-sm);
@@ -357,6 +377,7 @@ ${DASHBOARD_STYLES}
 }
 
 .savings-limit-line strong {
+  min-width: 0;
   font-variant-numeric: tabular-nums;
   overflow-wrap: anywhere;
   word-break: break-word;
@@ -435,6 +456,10 @@ ${DASHBOARD_STYLES}
   font-size: var(--font-sizes-2xl);
   font-weight: var(--font-weight-bold);
   color: var(--navy);
+  max-width: 100%;
+  overflow-wrap: anywhere;
+  word-break: break-word;
+  font-variant-numeric: tabular-nums;
 }
 
 .stat-change {
@@ -475,6 +500,71 @@ ${DASHBOARD_STYLES}
     padding: var(--space-md);
   }
 
+  .savings-management .table {
+    min-width: 620px;
+  }
+
+  .savings-recent-transactions-table {
+    min-width: 0 !important;
+  }
+
+  .savings-recent-transactions-table-wrap {
+    overflow: visible;
+  }
+
+  .savings-recent-transactions-table thead {
+    display: none;
+  }
+
+  .savings-recent-transactions-table,
+  .savings-recent-transactions-table tbody,
+  .savings-recent-transactions-table tr,
+  .savings-recent-transactions-table td {
+    display: block;
+    width: 100%;
+  }
+
+  .savings-recent-transactions-table tbody {
+    display: grid;
+    gap: var(--space-md);
+  }
+
+  .savings-recent-transactions-table tr {
+    border: 1px solid var(--border-light);
+    border-radius: var(--radius-md);
+    background: var(--white);
+    padding: var(--space-md);
+    box-shadow: var(--shadow-sm);
+  }
+
+  .savings-recent-transactions-table td {
+    border: none !important;
+    padding: var(--space-xs) 0 !important;
+    max-width: 100%;
+    overflow-wrap: anywhere;
+    word-break: break-word;
+  }
+
+  .savings-recent-transactions-table td::before {
+    content: attr(data-label);
+    display: block;
+    color: var(--text-muted);
+    font-size: var(--font-sizes-xs);
+    font-weight: var(--font-weight-semibold);
+    text-transform: uppercase;
+    letter-spacing: 0.4px;
+    margin-bottom: 2px;
+  }
+
+  .savings-recent-transactions-table .savings-amount-cell {
+    font-size: var(--font-sizes-sm);
+  }
+
+  .savings-recent-transactions-table .badge {
+    width: fit-content;
+    max-width: 100%;
+  }
+
   .premium-stat-card h3,
   .savings-management .card h4 {
     font-size: clamp(1.05rem, 4.8vw, 1.35rem);
@@ -495,6 +585,10 @@ ${DASHBOARD_STYLES}
     flex-direction: column;
     align-items: flex-start;
   }
+
+  .stat-card {
+    padding: var(--space-lg);
+  }
 }
 `;
 
